<section class="pagination">
        <% if (hasPreviousPage) { %> <a href="?page=<%= previousPage %>"><%=previousPage %></a><% } %>
        <a href="?page=<%= currentPage %>" class="active"><%=currentPage %></a>
        <% if (hasNextPage) { %> <a href="?page=<%= nextPage %>"><%=nextPage %></a><% } %>
</section>
<!-- If we use / in front of any link then be sure that it is starting from there only.That is it becomes the root url.But if we remove that then it defined by the last executed link as to what has to take its place. -->